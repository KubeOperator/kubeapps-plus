/**
 * @id example: stable/aerospike
 * @name example: aerospike
 * @author: maguohao
 */
export function splitName (id) {
    if (id == null || name == null) {
        return ''
    }
    let str = id.split('/')[0]
    return str
}
